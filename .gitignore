# HGC-Net-local Issue #59 CUDA environment and extension outputs.
.venv/
__pycache__/
pointnet2/pointnet2/build/
pointnet2/pointnet2/pointnet2_cuda*.so
runs/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
